<Application x:Class="FoxVoice.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="AppBackground" Color="#090B11"/>
        <SolidColorBrush x:Key="SidebarBackground" Color="#0B0E15"/>
        <SolidColorBrush x:Key="PanelBackground" Color="#11151F"/>
        <SolidColorBrush x:Key="PanelRaised" Color="#151B27"/>
        <SolidColorBrush x:Key="FieldBackground" Color="#0D111A"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#273142"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#37E7F5"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#8269FF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#F4F7FD"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#8D98AD"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#43E5B0"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#FF5C86"/>
        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource TextPrimary}"/><Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI"/></Style>
        <Style TargetType="Control"><Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI"/></Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/><Setter Property="Background" Value="{StaticResource PanelRaised}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/><Setter Property="BorderThickness" Value="1"/><Setter Property="Padding" Value="14,9"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
                <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource AccentBrush}"/></Trigger>
                    <Trigger Property="IsPressed" Value="True"><Setter TargetName="Root" Property="Opacity" Value="0.78"/></Trigger>
                    <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Root" Property="Opacity" Value="0.45"/></Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/><Setter Property="Background" Value="{StaticResource FieldBackground}"/><Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/><Setter Property="Padding" Value="11,8"/><Setter Property="CaretBrush" Value="{StaticResource AccentBrush}"/>
        </Style>
        <Style TargetType="ComboBox"><Setter Property="Foreground" Value="#11151F"/><Setter Property="Background" Value="#E9EEF8"/><Setter Property="Padding" Value="8,6"/></Style>
    </Application.Resources>
</Application>
